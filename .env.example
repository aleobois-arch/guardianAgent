# GuardianAgent Environment Variables
OPENAI_API_KEY=sk-...          # Required — GPT-5.6 access
GUARDIAN_ENV=development       # development | production
GUARDIAN_GATE_MODE=advisory    # advisory | warn | hard
SLACK_WEBHOOK_URL=             # Optional — Slack notifications
TEAMS_WEBHOOK_URL=             # Optional — Microsoft Teams notifications
